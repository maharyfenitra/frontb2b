"use client";
import { Header } from "@/lib";
import { MainPage} from "@/lib";
const Dashboard = () => {
  return (
    <div>
      <Header title="Dashboard" />
      <MainPage/>
    </div>
  );
};

export default Dashboard;
